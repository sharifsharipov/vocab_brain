<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Applied before Compose draws: keeps the launch window on the brand surface color. -->
    <style name="Theme.Vocabbrain" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
